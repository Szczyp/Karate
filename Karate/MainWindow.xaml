<Window x:Class="Karate.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Label Name="rubyLabel"/>
        <Label Grid.Row="1" Name="pythonLabel" Content="{Binding notify.DataValue}"/>
        <Button Grid.Row="2" Name="button" Click="Button_Click" Content="notify" Margin="30" />
        <ListView Grid.Row="3" Name="list" ItemsSource="{Binding list}" Margin="30,0,30,10"/>
    </Grid>
</Window>
